export type ITranscript = {
  id: string;
  name: string;
  examName: string;
  examSubject: string;
  examDate: Date;
  pointNumber: number;
  pointText: string;
  classification: string;
  createdAt: Date;
  updatedAt: Date;
};
