import StudentTranscriptView from 'src/sections/transcript/student-transcript/transcript-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Bảng điểm học sinh',
};

export default function Page() {
  return <StudentTranscriptView />;
}
